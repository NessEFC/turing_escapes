<%= @last_escape.name %>
<%= @last_escape.city %>
<%= image_tag @last_escape.image %>
<%= @last_escape.price %>
<%= @last_escape.description %>

<p>Total: $<%= @cart.total_price %></p>

<td><%= button_to "Remove Escape", cart_path({escape_id: @last_escape.id}), method: :delete, class: "btn orange-btn"%><td>
